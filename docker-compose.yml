# services:
#   db:
#     image: postgres:17
#     environment:
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_DB: postgres
#     ports:
#       - "7432:5432"
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U postgres -d postgres"]
#       interval: 5s
#       timeout: 5s
#       retries: 5
#       start_period: 5s
#     volumes:
#       - db_data:/var/lib/postgresql/data

#   api:
#     build: .
#     env_file: .env
#     environment:
#       DATABASE_URL: postgresql+asyncpg://postgres:postgres@db:5432/postgres
#     depends_on:
#       db:
#         condition: service_healthy
#     ports:
#       - "8000:8000"
#     command: uvicorn auth_app.main:app --host 0.0.0.0 --port 8000
#     restart: unless-stopped

# volumes:
#   db_data:



services:

  backend:
    build: .
    container_name: fastapi_cook

    ports:
      - "8000:8000"

    env_file:
      - .env

    depends_on:
      - postgres
      - redis

    volumes:
      - .:/code

    command: >
      sh -c "
      alembic upgrade head &&
      uvicorn app.main:app --host 0.0.0.0 --port 8000
      "

  postgres:
    image: postgres:17-alpine

    container_name: fastapi_postgres

    restart: always

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fastapi_cook

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data


  redis:
    image: redis:7-alpine

    container_name: fastapi_redis

    restart: always

    ports:
      - "6379:6379"


volumes:
  postgres_data: